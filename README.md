# FaktorialExperiments

Static browser experiments for Faktorial plan items.

## Pac-Man shell

Open `index.html` directly in a browser to run the static Pac-Man shell. No
backend, package install, or build step is required.
